# 모덕 네컷 — 작업 내역 및 할 일 (2026-09-17 기준)

다른 컴퓨터에서 이어서 작업할 때 이 파일부터 읽으세요.

## 저장소 / 배포
- GitHub: https://github.com/junho1-commits/modeok-4cut (공개)
- 배포 주소(GitHub Pages): https://junho1-commits.github.io/modeok-4cut/ — `main` 브랜치 루트에서 자동 배포, push 후 1~2분
- 2026-09-21 기준 전부 push 완료(bb8a88a). 로컬과 GitHub 이 같음.
- OneDrive 폴더에 `.git` 이 함께 동기화되므로 다른 PC 에서 OneDrive 동기화가 끝났으면 그대로 `git log` 로 확인 가능. 동기화가 안 됐으면 GitHub 에서 clone (단, 아래 미푸시 커밋 내용은 없음).

## 오늘 한 일 (커밋 순서)
1. `22c876a` 첫 커밋
   - 흐름 변경: 장수 선택 → 촬영(6초 카운트다운, **📸 바로 찍기** 버튼/Space) → (6·8장) 사진 선택 → 프레임 → 꾸미기(60초) → **인쇄 매수 1~4장 선택** → 인쇄 후 자동으로 촬영 화면 복귀
   - 인쇄: 매수만큼 페이지를 만들어 한 번의 `window.print()` 로 출력 (`#printArea` 에 img 여러 장, 페이지당 1장)
   - QR 전달 기능: 완성 사진을 Apps Script 로 POST → 학교 Drive `네컷사진` 폴더 저장 → 공유 링크를 QR 로 표시 (`assets/qr/qrcode.min.js`, MIT)
   - 정리: 중복 `source.png`·미참조 `reference-preview.png` 삭제, 참고 사진은 `참고자료/` 로 이동(.gitignore 로 공개 저장소 제외)
2. `9a4f13f` QR 업로드 주소를 `index.html` 의 `DEFAULTS.qrUrl` 에 기본값으로 내장 → 설정 없이 QR 표시
   - Apps Script 는 배포·검증 완료 (GET/POST 정상, Drive 링크 로그인 없이 열림)
3. `e076ddd` `실행.bat`·`테스트실행(창모드).bat` 수정 — 한글·공백 경로 때문에 크롬이 빈 탭만 열던 문제. 경로를 퍼센트 인코딩한 `file:///` URL 로 넘기도록 변경 (PowerShell 로 계산)
4. `d42c616` 문구 글꼴 9종 추가 (총 13종, 모두 OFL 내장): 개구쟁이·동글·큐트폰트·싱글데이·나눔손글씨 붓·도현·검은고딕·베이글팻·송명. `fonts.css` 가 18MB → 41MB

## 2026-09-21 한 일 (push 완료: 69321bd 장애 공감 캐릭터 3종, bb8a88a 행사 세팅 화면)
- 첫 화면을 **행사 세팅 화면**으로 교체: 학교/행사 이름·하단 문구·**제목 글꼴**(내장 13종, 기본 주아체)·날짜·촬영 장수·프레임 고정·스티커 첫 탭을 인쇄 미리보기 보며 설정 → `▶ 부스 시작`. 값은 `S.titleFont/shots/frameId/decoTheme` 로 저장(바꾸는 즉시 localStorage). 이후 촬영↔인쇄 반복, Esc 로 세팅 화면
- 인쇄물 학교 이름·하단 문구 글꼴: 맑은 고딕 → 세팅에서 고른 내장 글꼴(`titleFontSpec`, 동글은 1.35배 보정, 긴 이름은 폭에 맞춰 축소)
- 프레임 고정 시 `goFrame` 이 프레임 화면을 건너뜀. `allFrames()/frameById()` 로 사용자 PNG 프레임 포함 통일
- 비밀번호(1234) 6·8장 숨김 모드 제거 — 장수는 세팅에서 선생님이 정함. 고급 설정(S)에서 이름·문구·날짜 항목 제거(세팅 화면으로 이동)
- `scripts/check-app.cjs` 세팅 흐름 검사로 교체 (이 PC 에 Node 없음 — 브라우저에서 동작 확인함, 다른 PC 에서 `node scripts/check-app.cjs` 한 번 돌려볼 것)

## 2026-09-21~22 — 휴대폰판 + (선택) PC 인쇄 대기
- `shared/booth-core.js`: 규격·프레임·스티커 묶음·글꼴 목록·sharpen·drawPage 등 공통 코드를 `index.html` 에서 분리 (PC 는 `<script src="shared/booth-core.js">` 로 읽음, 동작 동일 확인)
- `mobile/`: 휴대폰 PWA. 세팅 → 촬영(앞/뒤 카메라, 큰 셔터) → 고르기 → 프레임(가로 스와이프) → 꾸미기(시간제한 없음, 한 손가락 이동·두 손가락 크기/회전·↻ 손잡이) → 매수 선택 → Apps Script 로 Drive 업로드(파일 이름 `네컷_시각_N장`, 교사가 Drive 에서 직접 출력, QR 없음, 💾 폰에 저장 버튼) → 10초 뒤 다음 팀. 고급 설정 `autoQueue` 를 켜면 `print:true` 로 보내 print-station 이 자동 인쇄(재배포 필요). 저장·공유(Web Share). 글꼴 4종 woff2(약 1MB), 캐릭터 WebP(약 1.5MB), manifest·sw.js·아이콘
- `print-station/index.html` + `인쇄대기.bat`: 4초마다 `?action=list` → `get` → `<img>`×매수 → `window.print()` → `done`. 옛 서버면 경고
- `qr-server/Code.gs`: `doGet` 에 list/get/done 추가, `doPost` 가 `print`/`copies` 를 받아 `print_…_x매수.jpg` 로 저장 → **재배포 필요** (설치방법.md 4번)
- 브라우저(로컬 서버 127.0.0.1:8765)에서 세팅·고르기·프레임·핀치·완성·보내기(가짜 서버)·인쇄 대기 로직(가짜 서버) 확인. 실제 폰·프린터는 아직

- 2026-09-22: 실제 폰 스크린샷 기준으로 꾸미기 화면 재배치 — 스티커 한 줄 가로 스크롤(고정 86px), ✏️ 문구 버튼(prompt), 스티커 줄 접기(`toggleSheet`), 안내는 3초 토스트, 캔버스가 남는 높이를 전부 차지

- 2026-09-22: 휴대폰 **사진 제출** — 매수 고르고 제출 → 서버(`Code.gs` submit)가 그날 1번부터 번호 매김(LockService, ScriptProperties `seq_YYYYMMDD`) → 파일 `MMDD-NNN_N장.jpg`, 응답 `number` 를 크게 표시. 옛 서버면 폰 로컬 번호(`S.seq`)로 대체. **재배포 필요**

- 2026-09-22: 7일 뒤 휴지통 자동 삭제(cleanup 트리거) 제거 — `Code.gs` 에서 cleanup/KEEP_DAYS 삭제, `removeCleanupTrigger` 로 기존 예약 해제. **재배포 + 편집기에서 removeCleanupTrigger 1회 실행 필요**

- 2026-09-22: 폰 번호(예비 순번)와 서버 파일명이 어긋나는 문제 → 폰은 서버가 돌려준 파일 이름에서 번호를 읽어 표시(`/-(\d{3})_/`), 못 받으면 "번호 없음" 경고. `?action=resetNumber` + 고급 설정 "오늘 번호 1번부터 다시" 추가. **재배포 필요**

## 당장 할 일
- [ ] Apps Script: 새 Code.gs 붙여넣기 → 편집기에서 `removeCleanupTrigger` 실행(자동 삭제 예약 해제) → 배포 관리 → 새 버전
- [ ] 실제 휴대폰(아이폰·안드로이드 각 1대)으로 https://junho1-commits.github.io/modeok-4cut/mobile/ 열어 카메라·핀치·드라이브 업로드·폰 저장 확인, Drive 에서 출력해 보기
- [ ] **Apps Script 재배포** (`qr-server/설치방법.md` 4번) — 사진 번호를 서버가 매기도록. 그 뒤 휴대폰에서 제출해 드라이브에 `0922-001_1장.jpg` 로 오는지 확인
- [ ] (자동 인쇄를 쓸 때만) 휴대폰 고급 설정 autoQueue 켜기 + `인쇄대기.bat`
- [ ] 아이폰에서 "홈 화면에 추가" 앱으로 열었을 때 카메라 허용이 되는지 확인 (안 되면 사파리에서 직접 열도록 안내)
- [ ] `node scripts/check-app.cjs` 실행해 세팅 흐름 검사 통과 확인 (Node 있는 PC)
- [x] 입학 캐릭터 4종(`animation_entrance_*`) 등록: `build-assets.cjs` 목록·`characters.js` 재생성·`check-app.cjs` 캐릭터 수 13·제작기록·README 반영 (2026-09-17)
- [ ] **화질 확인**: 촬영 화면 오른쪽 위(또는 설정 창)에 뜨는 "촬영 해상도"가 1280×960 이상인지 확인. 640×480 이면 카메라를 USB 3.0 포트에 직접 꽂고(허브 X) 다시 실행. 그래도 낮으면 카메라 자체 한계 → 카메라 교체 필요
- [x] GitHub push 완료 (Apps Script 업로드 URL 공개 승인함)
- [ ] **실제 프린터 테스트**: `테스트실행(창모드).bat` 로 열어 인쇄 매수 3장 선택 시 인쇄 대화상자에 "3페이지" 로 뜨는지, SELPHY 로 여백 없이 나오는지 확인 (인쇄 영역 구조를 바꿨음)
- [ ] **QR 실물 테스트**: `실행.bat` 으로 한 번 찍고 완성 화면의 QR 을 휴대폰(모바일 데이터)으로 찍어 Drive 사진이 열리는지 확인
- [ ] `실행.bat` 이 다른 PC 에서도 정상 실행되는지 확인 (PowerShell 필요 — Windows 기본 포함)

## 검토 중 / 선택 사항
- 글꼴 **동글**은 디자인상 작게 보임 → 필요하면 `drawSticker` 에서 동글만 1.3배 확대
- 글꼴 추가 후보(용량 커서 보류): 감자꽃·하이멜로디·스타일리시(각 10~12MB)
- 결과 화면 인쇄 후 자동 복귀 시간: 기본 8초, QR 켜져 있으면 30초 (`doPrint` 안 `left`)
- 배포 주소를 앱 모드·자동 인쇄로 여는 `실행(웹).bat` 만들기 (USB 없이 주소만으로 운영)
- 저장소를 비공개로 바꾸면 무료 계정에선 GitHub Pages 가 꺼짐

## 알아둘 것
- 인화지 설정(`S.paper`, `PAPERS`, `applyPaper`): 셀피 100×148 / 4×6인치 102×152. 캔버스는 1181×1748 그대로 두고 인쇄 CSS(`@page`, img 크기)만 `<style id=paperStyle>` 로 바꿈. 코닥 PD460 은 윈도우 드라이버가 없어(iOS/Android 앱 전용) PC 자동 인쇄 불가 — 쓰지 말 것. 4×6 설정은 PC 인쇄 되는 다른 프린터용이며 실물 테스트는 아직 안 함. 코닥용은 완성 화면 💾 저장 → '코닥 4×6용'(`makeKodakDataUrl`, 1200×1800, 높이 맞춰 3% 확대 후 좌우 8px 크롭). 가진 PD460 은 인쇄가 왼쪽으로 약 8% 밀리는 개체 불량 — 앱 쪽 안전 여백은 넣었다가 뺐음(기기 문제로 판단). 코닥 앱에서 □(테두리) 모드로 뽑으면 안 잘림 파일을 휴대폰으로 옮겨 코닥 앱에서 인쇄
- 화질 관련(2026-09-17): 카메라를 최대 해상도로 열고, 저장된 카메라를 못 찾아도 640×480 으로 떨어지지 않게 함(`openCamera`). 실제 해상도는 촬영 화면·설정 창에 표시(`showCamRes`, 세로 960px 미만이면 노란 경고). 촬영 순간 바로 사진 칸 크기(535×690)로 줄이고 언샤프 마스크로 윤곽을 살림(`captureFrame`→`sharpen`, 세기 `SHARPEN`=0.6, 0 이면 끔). 인쇄는 PNG(`printDataUrl`), 저장·QR 은 JPEG 0.95. 얼굴이 뭉개지는 건 웹캠이 어두울 때 노이즈를 지우는 탓 → 조명이 근본 해결
- 설정(S 키)은 브라우저 localStorage 에 저장 → `실행.bat`(전용 프로필)과 배포 주소는 각각 따로 저장됨
- 예전 저장값 마이그레이션: `S.ver` (2: 카운트다운 6초, 3: QR 기본 주소 채움)
- 검사: `node scripts/check-app.cjs` (DOM 모의 검사, 브라우저·프린터 검사 아님). 에셋 바꾸면 `node scripts/build-assets.cjs`
- 글꼴 추가 순서: ttf + OFL.txt 를 `assets/fonts/` 에 → `scripts/build-assets.cjs` fonts 배열 → `index.html` STICKER_FONTS/FONT_NAMES (같은 순서) → build-assets 실행
- 같은 폴더를 두 세션(또는 두 PC)에서 동시에 편집하면 OneDrive 충돌 파일이 생김. 한 번에 한 곳에서만 작업하고 커밋/push 로 넘길 것
- 문서: `사용설명서.md`(운영), `qr-server/설치방법.md`(QR 서버), `assets/README.md`(에셋)
