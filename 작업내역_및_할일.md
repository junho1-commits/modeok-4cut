# 모덕 네컷 — 작업 내역 및 할 일 (2026-09-17 기준)

다른 컴퓨터에서 이어서 작업할 때 이 파일부터 읽으세요.

## 저장소 / 배포
- GitHub: https://github.com/junho1-commits/modeok-4cut (공개)
- 배포 주소(GitHub Pages): https://junho1-commits.github.io/modeok-4cut/ — `main` 브랜치 루트에서 자동 배포, push 후 1~2분
- **주의: 로컬 커밋 4개가 아직 push 되지 않음** (아래 "당장 할 일" 참고). GitHub 에는 첫 커밋(22c876a)만 올라가 있음.
- OneDrive 폴더에 `.git` 이 함께 동기화되므로 다른 PC 에서 OneDrive 동기화가 끝났으면 그대로 `git log` 로 확인 가능. 동기화가 안 됐으면 GitHub 에서 clone (단, 아래 미푸시 커밋 내용은 없음).

## 오늘 한 일 (커밋 순서)
1. `22c876a` 첫 커밋 (GitHub 에 올라간 버전)
   - 흐름 변경: 장수 선택 → 촬영(6초 카운트다운, **📸 바로 찍기** 버튼/Space) → (6·8장) 사진 선택 → 프레임 → 꾸미기(60초) → **인쇄 매수 1~4장 선택** → 인쇄 후 자동으로 촬영 화면 복귀
   - 인쇄: 매수만큼 페이지를 만들어 한 번의 `window.print()` 로 출력 (`#printArea` 에 img 여러 장, 페이지당 1장)
   - QR 전달 기능: 완성 사진을 Apps Script 로 POST → 학교 Drive `네컷사진` 폴더 저장 → 공유 링크를 QR 로 표시 (`assets/qr/qrcode.min.js`, MIT)
   - 정리: 중복 `source.png`·미참조 `reference-preview.png` 삭제, 참고 사진은 `참고자료/` 로 이동(.gitignore 로 공개 저장소 제외)
2. `9a4f13f` QR 업로드 주소를 `index.html` 의 `DEFAULTS.qrUrl` 에 기본값으로 내장 → 설정 없이 QR 표시
   - Apps Script 는 배포·검증 완료 (GET/POST 정상, Drive 링크 로그인 없이 열림)
3. `e076ddd` `실행.bat`·`테스트실행(창모드).bat` 수정 — 한글·공백 경로 때문에 크롬이 빈 탭만 열던 문제. 경로를 퍼센트 인코딩한 `file:///` URL 로 넘기도록 변경 (PowerShell 로 계산)
4. `d42c616` 문구 글꼴 9종 추가 (총 13종, 모두 OFL 내장): 개구쟁이·동글·큐트폰트·싱글데이·나눔손글씨 붓·도현·검은고딕·베이글팻·송명. `fonts.css` 가 18MB → 41MB

## 당장 할 일
- [ ] **GitHub push 여부 결정**: 미푸시 커밋에 Apps Script 웹앱 URL(누구나 알면 Drive 폴더에 *업로드만* 가능, 읽기·삭제 불가, 7일 후 자동 삭제)이 포함됨. 공개해도 괜찮으면 `git push origin main`. 싫으면 `DEFAULTS.qrUrl` 을 비우고 앱 설정에서 직접 입력하는 방식으로 되돌린 뒤 push.
- [ ] **실제 프린터 테스트**: `테스트실행(창모드).bat` 로 열어 인쇄 매수 3장 선택 시 인쇄 대화상자에 "3페이지" 로 뜨는지, SELPHY 로 여백 없이 나오는지 확인 (인쇄 영역 구조를 바꿨음)
- [ ] **QR 실물 테스트**: `실행.bat` 으로 한 번 찍고 완성 화면의 QR 을 휴대폰(모바일 데이터)으로 찍어 Drive 사진이 열리는지 확인
- [ ] `실행.bat` 이 다른 PC 에서도 정상 실행되는지 확인 (PowerShell 필요 — Windows 기본 포함)

## 검토 중 / 선택 사항
- 글꼴 **동글**은 디자인상 작게 보임 → 필요하면 `drawSticker` 에서 동글만 1.3배 확대
- 글꼴 추가 후보(용량 커서 보류): 감자꽃·하이멜로디·스타일리시(각 10~12MB)
- 결과 화면 인쇄 후 자동 복귀 시간: 기본 8초, QR 켜져 있으면 30초 (`doPrint` 안 `left`)
- 배포 주소를 앱 모드·자동 인쇄로 여는 `실행(웹).bat` 만들기 (USB 없이 주소만으로 운영)
- 저장소를 비공개로 바꾸면 무료 계정에선 GitHub Pages 가 꺼짐

## 알아둘 것
- 설정(S 키)은 브라우저 localStorage 에 저장 → `실행.bat`(전용 프로필)과 배포 주소는 각각 따로 저장됨
- 예전 저장값 마이그레이션: `S.ver` (2: 카운트다운 6초, 3: QR 기본 주소 채움)
- 검사: `node scripts/check-app.cjs` (DOM 모의 검사, 브라우저·프린터 검사 아님). 에셋 바꾸면 `node scripts/build-assets.cjs`
- 글꼴 추가 순서: ttf + OFL.txt 를 `assets/fonts/` 에 → `scripts/build-assets.cjs` fonts 배열 → `index.html` STICKER_FONTS/FONT_NAMES (같은 순서) → build-assets 실행
- 같은 폴더를 두 세션(또는 두 PC)에서 동시에 편집하면 OneDrive 충돌 파일이 생김. 한 번에 한 곳에서만 작업하고 커밋/push 로 넘길 것
- 문서: `사용설명서.md`(운영), `qr-server/설치방법.md`(QR 서버), `assets/README.md`(에셋)
